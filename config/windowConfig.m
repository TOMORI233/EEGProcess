windowBase0 = [-200, 0];
windowOnset = [0, 250];
windowBase = 1000 + interval + [-200, 0];

windowChangePeak = 1000 + interval + [50, 170];
windowChangeTrough = 1000 + interval + [170, 300];

windowChange = 1000 + interval + [50, 300];
% windowChange = windowChangePeak;
% windowChange = windowChangeTrough;